{
  "name": "pathwise",
  "private": true,
  "scripts": {
    "db:start": "node scripts/mongo-mac.js start",
    "db:stop": "node scripts/mongo-mac.js stop",
    "db:status": "node scripts/mongo-mac.js status",
    "dev": "concurrently -n frontend,backend,ai -c cyan,green,magenta \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\" \"npm --prefix ai-engine run dev\"",
    "dev:mac": "node scripts/mongo-mac.js start && npm run dev",
    "seed": "npm --prefix backend run seed",
    "build": "npm --prefix backend run build && npm --prefix ai-engine run build && npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
